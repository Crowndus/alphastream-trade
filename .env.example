# Deriv OAuth Client ID (required for login/signup to work).
# Register your app at developers.deriv.com to get this.
# This is NOT a secret — it identifies the app publicly; the actual
# security comes from PKCE + an exact redirect URL match on Deriv's side.
OAUTH_CLIENT_ID=your_oauth_client_id
